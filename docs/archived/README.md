# Archived design docs

Historical design/implementation notes from the **original Rust/Tauri** version
of HyperCapslock. The app is now a native Swift (SwiftUI + AppKit) port that
faithfully reproduces these designs — code references map to
`HyperCapslock/Sources/`. Kept for design history; not maintained against the
current implementation.
